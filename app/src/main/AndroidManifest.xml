<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owoshopkeeperpanel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SetPinActivity"></activity>
        <activity android:name=".ForgetVerifyPhoneActivity" />
        <activity android:name=".ForgetPinOtpActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".LocationFromMap" />
        <activity android:name=".AfterRegisterActivity" />
        <activity android:name=".TermsConditionsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".RegisterWithOTPActivity" />
        <activity android:name=".RegisterFrontActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DummyPendingShopActivity" />

        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>